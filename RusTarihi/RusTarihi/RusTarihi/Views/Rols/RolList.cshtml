@model IEnumerable<RusTarihi.Models.Rols>

@{
    ViewBag.Title = "Rol Listesi";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<a class="btn btn-primary" href="/Rols/RolCreate"> Rol Ekle</a>

<div class="row">
    <div class="col-lg-12">
        <section class="panel">
            <header class="panel-heading">
                <h1 class="text-center"><b style="color:red">@ViewBag.Title</b></h1>
            </header>

            <table class="table table-striped table-advance table-hover">
                <tbody>
                    <tr>
                        <th><i class="icon_pens"></i> Rol Adı</th>
                        <th><i class="icon_tag"></i> Seviye</th>
                        <th><i class="icon_tools"></i> </th>
                    </tr>

                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Name)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Seviye)
                            </td>
                            <td>
                                <div class="btn-group">
                                    <a class="btn btn-primary" href="/Rols/RolCreate"><i class="icon_plus_alt2"></i></a>
                                    <a class="btn btn-success" href="/Rols/RolEdit/@item.ID"><i class="icon_check_alt2"></i></a>
                                    <a class="btn btn-danger" href="/Rols/RolDelete/@item.ID"><i class="icon_close_alt2"></i></a>
                                </div>
                            </td>
                        </tr>
                    }

                </tbody>
            </table>
        </section>
    </div>
</div>